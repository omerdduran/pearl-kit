# Changelog

All notable changes to this project will be documented in this file.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versioning: [SemVer](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Fixed

- fix(select): `Component.onCompleted` no longer clobbers an external `currentIndex` binding when it already points to a valid `"item"` row. Previously the unconditional `currentIndex = i` imperative assignment deleted bindings (Qt's "imperative write overrides binding" rule), freezing the index on the first valid item and causing caller-side bindings like `currentIndex: model.findIndex(...)` to stop tracking source-property changes. Fallback to the first valid item still runs when `currentIndex < 0` (unselected/placeholder) or sits on a header/separator row, preserving the original behavior for callers that don't supply their own `currentIndex`. Two new regression tests in `tests/test_select.py` cover both branches.

### Changed

- refactor(tokens): drop `readonly` from `Tokens.qml` palette tables (`_bg`, `_fg`, `_primary`, …) so downstream consumers can override them at runtime. Defaults unchanged — existing consumers see no visual difference. Stage 1 of the JSON-driven theming refactor.
- refactor(tokens): convert `radius`, `space`, `font`, `shadow`, `motion` from nested `QtObject` children with readonly inner fields to plain `property var` JS objects. Existing dot-access (`Tokens.radius.md`, `Tokens.space.x4`, `Tokens.font.size.sm`) is unchanged. Reactivity contract: bindings re-evaluate when the WHOLE sub-object is reassigned, so consumers must NEVER mutate inner fields directly — go through `applyTheme()` (which always reassigns via `Object.assign`). Stage 2 of the JSON-driven theming refactor.

### Added

- feat(tokens): `font.size` display/hero rungs (`xxxl` 28, `display` 30, `displayLg` 36, `hero` 44, `heroLg` 48) for large titles & splash headings above the body scale.
- feat(radio): `RadioButton` component — 18px circular indicator with an animated filled inner dot, plus an inline label. Unlike `CheckBox` (label-less, composed externally), the radio renders its own `text` because consumers carry labels. Token-styled (border `input` / checked `primary`, dot `primaryForeground`), `error` state, and `PearlFocusRing` consistent with `CheckBox`/`Toggle`. `autoExclusive` / `ButtonGroup` inherited from `T.RadioButton`. Tests in `tests/test_radiobutton.py`.
- feat(tokens): `Tokens.applyColors(palette)` — runtime palette override hook. Pass an object whose keys are palette table names (`_bg`, `_primary`, …) and values are 3-element arrays indexed by mode. Unspecified tables keep their defaults. Bound `background`/`primary`/etc. colors re-evaluate automatically. Enables DALI to override pearl-kit's shadcn-flavored defaults with values sourced from its Penpot design system.
- feat(tokens): `Tokens.applyTheme(theme)` — full-coverage runtime override hook. Accepts `{ palette, radius, space, font, shadow, motion, anatomy, crosshair }` (any subset). Sub-objects shallow-merge with current values (deep-merge for `font.size` and `font.weight`). Always reassigns the whole sub-property to trigger QML binding refresh. Single first-class API for downstream theming.
- feat(tokens): new `Tokens.anatomy` and `Tokens.crosshair` token slots — empty objects by default, populated by downstream consumers via `applyTheme()`. Enables domain-specific token namespaces (e.g. DALI's anatomy/crosshair colors) to live alongside role-based shadcn tokens without polluting the OSS-default state.

### Added — Categorized showcase

- feat: `ShowcaseTile` — Card-based sample container exported under `PearlKit 1.0`. Properties: `label` (variant name shown in the footer), `description` (optional muted line), `sample` (default property — any QML item rendered as the demo). All chrome reads from `Tokens` (border, radius, padding, footer typography), no hard-coded hex / px. Defaults to `Layout.fillWidth: true` so it flows into a `GridLayout` without per-tile boilerplate.
- feat: `ShowcasePage` — abstract base for showcase category pages. Properties: `title`, `subtitle` (optional), `gridPreset` (`single-col` | `two-col` | `four-col` | `flow`), and the default property routes children into a `GridLayout` whose column count is enforced by the preset. Invalid presets fall back to `single-col` with a console warning. Reserves a `codeSnippet` property in the API surface (hidden in v1) so a future v2 copy-paste-snippet feature does not break the abstract base contract.
- feat: `CollapsibleNavGroup` — sidebar grouping primitive built on `NavItem` + `Group`. Properties: `header` (uppercase mono label), `expanded` (bool, default `true`), `persistKey` (string, optional — when non-empty mirrors expanded state to `QSettings` under category `PearlKit.CollapsibleNavGroup.<persistKey>` via `Qt.labs.settings`). Toggling animates the body's clip height; keyboard `↑`/`↓` traverses children when expanded.
- feat: `examples/showcase/` — new categorized showcase shell replacing the monolithic `examples/gallery.py`. `Showcase.qml` is a two-pane `ApplicationWindow` (sidebar with seven `CollapsibleNavGroup`s × 21 `NavItem`s, content pane is a `Loader` that swaps in the active category's QML page). Last-selected category persists to `QSettings`. Run with `uv run python examples/showcase/showcase.py`. Twenty-one category pages live under `examples/showcase/pages/` covering every shipping component grouped by function (Foundations, Form Primitives, Layout & Structure, Navigation, Feedback, Data Display, Domain).

### Removed

- breaking: `examples/gallery.py` is removed. The new categorized showcase under `examples/showcase/` is the single source of truth for "what does pearl-kit look like." Anyone scripting against the old single-page gallery should adapt to the new shell.

### Fixed

- fix(NumberedNavItem): drop the `Behavior on color` 150ms `ColorAnimation` entirely. Hover state changes re-evaluate the `_bg` binding even when the resolved value is unchanged (e.g. on the active item where `_bg` is locked to `"#EFF6FF"`); Qt treats every re-evaluation as a property assignment, so the Behavior animated `"#EFF6FF" → "#EFF6FF"` and produced a visible RGB-interpolation flicker. Hover background is now snap-on/snap-off — no animation.
- fix(DataTable): cell text now elides instead of overflowing onto neighboring columns when the column is narrower than the text's `implicitWidth`. Both header and body cell `Text` items are anchored with `anchors.fill: parent` and use `horizontalAlignment` / `verticalAlignment` (preserving the existing `align` semantics) plus `elide: Text.ElideRight` (default). Previously the `Text` was anchored only to one edge of the parent `Item`, leaving its width unconstrained — so long content (e.g. file paths in monospace) painted on top of the next column when the table or window shrank.
- feat(DataTable): column definitions accept an optional `elide` field (e.g. `{ key: "path", elide: Text.ElideMiddle }`) to override the default `Text.ElideRight` per column. Useful for path-style columns where preserving head + tail (filename) is more informative than truncating the right side.

## [0.4.0] — 2026-04-19

### Fixed

- fix: `DetachableTabView.tabByKey()` now also searches floating tabs. Previously, after a tab was detached and reparented into its `FloatingWindow._contentArea`, it was no longer in `_host.children` — so `tabByKey()` (and therefore `dockTabByKey()`) could not find it, and docking-by-key became a silent no-op. `tabByKey()` now falls back to scanning `_floatingMap[*].content` after exhausting docked tabs.
- fix: `DetachableTabView._floatingMap` mutations (`map[key] = win`, `delete map[k]`) did not trigger QML property change notifications, so the `floatingCount` binding never re-evaluated after detach / dock / `closeAllFloating()`. Attach / detach / close-all now reassign `_floatingMap` to a new object so the readonly `floatingCount` binding updates correctly.
- build: exclude `src/pearl_kit/_viewports/_vtk_viewport_item.py` from strict `pyright` because VTK is an optional runtime dependency without type stubs. Narrow `# pyright: ignore[...]` comments on two additional PySide6 stub gaps (`qmlRegisterType` accepting `str` for `qml_name`, `Property("QImage", ...)` accepting the string type name).
- ci: format-check the four overlay / viewport source files added in the v0.3.0 → v0.4.0 tranche (`_image_viewport_item.py`, `test_angle_arc.py`, `test_rect_select.py`, `test_text_annotation.py`). Unblocks CI on `main`.

### Added — DALI planning workspace (tranche 4: measurement overlays)

- feat: `RectSelect` — drag-to-select rectangle overlay. When `active: true`, a full-surface MouseArea tracks press-drag-release and renders a live rectangle with orange fill + stroke. On release emits `selected(x1, y1, x2, y2)` in image-space (axis-aligned, min/max normalised). Tiny rects (< 2 px on either axis) emit `cancelled()` instead — avoids accidental single-click triggers. No internal state persists after release.
- feat: `ArchPath` — dental arch polyline + draggable control-point handles for the axial viewport. Consumer owns the `points: [{x, y}, ...]` model; in `editing: true` mode each control point renders as a 14 px draggable circle that emits `pointMoved(index, x, y)` during drag. Read-only mode drops handles in favour of compact dots. Preview mode (`preview: true`) switches to dashed stroke. No spline interpolation — DALI's panoramic generation handles the math downstream; this component is the edit surface.
- feat: `Arrow` — 2-point arrow annotation overlay. Shape + PathLine shaft + filled triangular head. Preview mode drops the head and switches to dashed. Includes inline "×" delete button near the head. Emits `removed()` and `clicked()`.
- feat: `TextAnnotation` — anchored text label at image-space `(x_pos, y_pos)`. Two modes: `editing: true` shows a focused `TextInput` (Enter → `committed(text)`, Escape → `cancelled()`); `editing: false` renders a static label with a "×" delete button. Consumer decides when to flip mode (e.g. on first click in text-tool → editing, on commit → static).
- feat: `AngleArc` — 3-point angle measurement overlay. Two arms from a vertex to two endpoints + degree label chip + inline "×" delete. Dashed rubber-band preview mode. `angle` property is precomputed by the consumer (bridge-side `compute_angle_degrees`); component is purely presentational. Emits `removed()` and `clicked()`. Used by DALI's planning workspace when the angle measure tool is active.
- feat: `DistanceLine` — 2D distance measurement overlay for `ImageViewport`. Renders a line between two image-space points with an axis-aligned midpoint label chip (`"12.3 mm"`) and a 14 px circular delete button next to it. Preview mode (`preview: true`) draws a dashed rubber-band stroke without the delete affordance, suitable for the interim line shown between the first and second click. Emits `removed()` and `clicked()`. Used by DALI's planning workspace when the measure tool is active.

### Added — Experimental: DALI onboarding (tranche 1)

- feat: `StepCircles` — horizontal step indicator with 24×24 numbered circles, hairline connectors, and `labelMode: "active" | "all" | "none"`. Done steps render a check, active step gets accent border + accent number, upcoming steps stay neutral. Emits `stepClicked(int)` for any reachable step. Used in the compact onboarding-card header.
- feat: `StepRail` — vertical step list for the editorial onboarding rail. Each row is a 28 px circle column + label/sub column with a 1 px hairline between rows; the active row gets a soft accent halo around its circle. Emits `stepClicked(int)`.
- feat: `FeatureBullet` — welcome-step list row: 32×32 outlined icon square + medium title + optional secondary description. Static read-only display atom, every color/size exposed as a property.
- feat: `SignaturePad` — profile-step "Tap to sign" affordance: 64 px tall dashed rectangle that renders a mono `DRAW SIGNATURE` placeholder when unsigned and cursive ink (Dancing Script) when signed, with a side outline button (`OPEN CANVAS` / `REDRAW`). Visual-only — actual signature capture is the consumer's responsibility. Emits `requestRedraw()`.
- feat: `SampleCasePreview` — ready-step 140 px dark hero rectangle: layered radial-style gradients + faux volumetric blob + N implant marker bars (configurable via `markerCount`) + mono chips top-left/top-right. `default property alias overlayContent` lets consumers replace the decoration with a real CBCT thumbnail.
- feat: `FactGrid` — welcome-step 2-column stat grid: bordered container with 1 px hairlines (parent border + per-cell white fill spaced by 1 px row/column gaps). Each cell renders mono key + medium value + optional sub. `columns: int` for arbitrary N-column layouts.
- feat: `SettingsToggleCard` — toggle row card for the AI/privacy step. Title (+ optional badge chip) on top, optional description below, `Toggle` on the right. `highlighted: true` switches to blue-tinted border + bg; `locked: true` disables the Toggle and fades the card (used for HIPAA → cloud-inference lock). Emits `toggled(bool)`.
- feat: `SummaryGrid` — ready-step "YOUR SETUP" card: grey-surface bordered rectangle with a mono eyebrow and a 2-column mono key-value grid below. Each entry can override its value color via `valueColor` for OK/warn coloring.
- feat: `SampleCaseCard` — composite case card: `SampleCasePreview` hero on top + meta block (serif title + caseId, mono meta line, hairline + N metric chips) at the bottom. Forwards `previewTopLeftLabel` / `previewTopRightLabel` / `previewMarkerCount` / `previewHeight` to the embedded preview. CTA buttons stay in consumer-land.
- feat: `OnboardingNavFooter` — onboarding footer band: ghost back (left) + mono `STEP X / Y` counter (center) + skip + optional secondary outline + primary accent (right). Every label, signal, and enabled flag is a property so consumers can drive both intermediate and final steps. Emits `back()`, `skip()`, `primary()`, `secondary()`.
- feat: `examples/gallery.py` — new `Experimental · Onboarding — atoms` and `… — compositions` sections under the Settings tranche, demonstrating each component with representative payloads.

### Added — DALI planning workspace (tranche 3: render surfaces)

- feat: `VTKViewportItem` (Python `QQuickPaintedItem`) + `VTKViewport` (QML wrapper) — native host for a VTK render pipeline inside a QtQuick scene. Owns an offscreen `vtkGenericOpenGLRenderWindow`, forwards pointer / wheel events to a `vtkGenericRenderWindowInteractor`, and emits `firstRenderCompleted` / `renderFinished(ms)` for LOD heuristics. Mirrors DALI's `_GatedVTKWidget` gating (`allow_rendering` / `block_rendering`) so the first paint doesn't stall the main thread while GPU mappers initialise. VTK is an **optional** dependency — `vtk_available()` reports backend status and the item paints a graceful "backend unavailable" placeholder when VTK is not installed. Registered as `PearlKit.Viewports.VTKViewportItem 1.0` and composed in `PearlKit.VTKViewport 1.0` with the usual viewport chrome (title chip, orientation cube, bottom info caption, overlay slot).
- feat: `ImageViewportItem` (Python `QQuickPaintedItem`) + `ImageViewport` (QML wrapper) — QImage-backed 2D slice surface for axial / coronal / sagittal views. Consumers (e.g. DALI's `ImageViewer`) push pre-rendered frames via `set_frame(QImage)`; the item handles pan / zoom / fit, emits pointer events in **image coordinates** so existing overlay math keeps working unchanged. Middle-drag pans, Ctrl+wheel zooms, bare wheel emits `wheelScrolled(delta, modifiers)` for consumer slice-scroll logic. Registered as `PearlKit.Viewports.ImageViewportItem 1.0` and composed in `PearlKit.ImageViewport 1.0` with the existing Viewport2D chrome (axis-colored title chip, slice counter, crosshair, scroll track, overlay slot).
- feat: `register_qml()` now also registers the viewport `QQuickItem` subclasses via `qmlRegisterType` under the separate `PearlKit.Viewports 1.0` module. Separate module keeps the VTK / image-hosting cost off consumers that don't need them. Registration is idempotent.
- feat: `examples/planning_viewports_preview.py` — runnable 2x2 grid showing all three planes + a VTK cube viewport. Synthetic gradient frames at ~25 fps exercise pan / zoom / scroll. Works without VTK (placeholder paint).

### Added

- feat: `MetaRow` — new `elideMode: int` (defaults to `Text.ElideNone`) and `valueGap: int` (defaults to `12`) properties. The value `Text` is now anchored between the label and the right edge (previously right-anchored only), so `Text.ElideRight` / `Text.ElideMiddle` properly truncate long values (file paths, procedure names) inside a fixed-width row instead of overflowing the parent. Call sites that do not set `elideMode` keep the previous right-anchored, non-eliding layout. Used by DALI's Case Library `SELECTED STUDY` panel to elide long vendor DICOM paths with a hover tooltip revealing the full value.

### Changed

- `Render3D` — the `bottomLeftLabel` `Text` node is now hidden when `bottomLeftLabel` is set to the empty string. The default value (`"◉ canal detected"`) and all existing behavior are preserved; this just lets consumers opt out of the decorative canal-hint label without editing the component. Used by DALI's Case Library sidebar once a real CBCT render replaces the decorative mockup.

### Added

- feat: `Thumb` and `Render3D` gain an `imageSource: url` property — when set, an `Image { fillMode: PreserveAspectCrop; asynchronous: true; cache: false }` replaces the decorative radial-gradient placeholders (and, on `Render3D`, the faux implant-marker + canal-hint). Labels and borders stay visible so real CBCT thumbnails match the chrome of the Figma placeholder. Empty `imageSource` → legacy mockup look unchanged. Used by DALI to bind VTK-offscreen-rendered CBCT slice / volume thumbnails into the Case Library's SELECTED STUDY panel.

### Added — Experimental: DALI planning workspace (tranche 1)

- feat: `Breadcrumb` — mono path trail with per-segment color (past / penultimate / current), configurable separator glyph. Extracted from the DALI top-bar breadcrumb.
- feat: `SegmentedControl` — three-variant grouped picker (`pill` for mode tabs, `bordered` for dense variant switchers, `solid` for compact option grids). Supports `columns: int` for N-column grid layout.
- feat: `SaveIndicator` — colored dot + mono timestamp for autosave / connection states (`saved` / `saving` / `error` / `offline`).
- feat: `PatientStrip` — compound: optional back affordance + `Breadcrumb` + vertical divider + serif name + mono meta. 48 px fixed height.

### Added — Experimental: DALI planning workspace (tranche 2)

- feat: `StatTile` — mono eyebrow + serif numeral + mono subtitle summary tile. Three sizes (`sm` / `md` / `lg`) and three tones (`neutral` / `warn` / `success`) for stat strips and editorial spreads.
- feat: `DataTable` — PACS-style dense table with configurable column schema, fixed or flex-width columns, per-cell color / weight overrides via `<key>Color` / `<key>Weight` row keys.
- feat: `Chip` — small clickable mono pill with `soft` (tinted) and `outline` (ink-bordered) variants. Interactive `AbstractButton` — emits `clicked`.

### Added — Experimental: DALI planning workspace (tranche 3)

- feat: `ToolButton` — 32×32 icon-only palette button with active (blue-tinted) state and native tooltip (`Label (Hotkey)`). Wraps `T.Button`; toggles `checked` on click and emits `toggled()`.
- feat: `Viewport2D` — CBCT slice imaging surface (axial / coronal / sagittal) with radial atmospheric gradient, axis-colored crosshairs (`#3B82F6` / `#F87171` / `#34D399`), header chip, slice counter, voxel caption, and optional right-edge slice scroll track. Default-property slot accepts absolute-positioned overlays.
- feat: `OrientationCube` — 52×52 R/L/S/I anatomical orientation marker with configurable labels and center dot glyph.
- feat: `DensityBar` — horizontal red→yellow→green→blue gradient bar with a 1 px value needle. Accepts arbitrary `[from, to]` range and unit label (defaults to `HU`).
- feat: `SafetyRow` — per-structure safety-distance row: label + mono value + 3 px threshold bar with a 50 % min-safety tick + mono caption. Green / amber tone driven by `value >= min`.
- feat: `SubjectRow` — selectable list row with a colored ID square (28×28), title, mono caption, and optional warn status dot with glow. `AbstractButton` — emits `clicked`.
- feat: `BipolarSlider` — symmetric ±range slider built on `T.Slider` with a visible center tick and mono `−max · 0 · +max` labels.

### Added — Experimental: DALI planning workspace (tranche 4)

- feat: `ChecklistItem` — tap-to-ack checklist row with 16×16 checkbox (green fill on ack), severity dot + mono label (`ACTION` / `CONFIRM` / `NOTE`), title, wrapped body. Fades row to 60 % opacity when acknowledged; emits `toggled()` on click.
- feat: `IconBadge` — 20×20 rounded square with gradient or solid tinted fill (`info` / `warn` / `success` / `primary`) and a centered icon. Standalone atom plus building block for `AISuggestionCard`.
- feat: `AISuggestionCard` — compound tinted card with `IconBadge` header + bold title + mono model tag + wrapped body + primary `Button` action. Hardcoded `#EFF6FF` / `#DBEAFE` to mirror the DALI plan rail exactly.

### Added — Experimental: DALI planning workspace (tranche 5)

- feat: `PullQuote` — editorial pull-quote: large serif `"` glyph + italic wrapped text with a 720 px max-width. Configurable glyph / colors.
- feat: `EditorialHero` — full-bleed section header with mono uppercase eyebrow + 56 px serif headline (max 720 px) + 15 px UI sub-line (max 680 px) + top-right default-property slot for action affordances.
- feat: `SectionNumeral` — sibling of `SectionLabel` that preformats a numeral prefix (`I`, `II`, …) joined to the label by ` · ` — mono uppercase 10 px, letter-spacing 1.5.

### Added — Experimental: DALI planning workspace (tranche 6)

- feat: `ChatMessage` — single chat turn with user / ai styling: user = blue mono author label + muted `#F7F8FA` panel bubble; ai = grey label + 2 px left-border text block. Optional outline `Chip` action rendered below the body; emits `action()` on click.
- feat: `ChatComposer` — compound `Input` + mono uppercase dark send button. <kbd>Enter</kbd> submits; empty text is ignored; input is cleared on submit. Emits `submitted(text: string)`.
- feat: `SubjectCard` — balanced-variant compound spec card: mono identifier / tooth meta + serif 18 px brand + mono status chip + 4-column parameter grid + safety strip with tone-colored metrics.
- feat: `ApproveBar` — footer: mono count label + flex progress bar + outline export button + primary sign button. Sign button disabled until `acknowledged >= total`; flips to green with a signed-sent label when `signed: true`. Emits `exportRequested()` / `signRequested()`.

### Added — Experimental: DALI settings (tranche A — form primitives)

- feat: `SettingsRow` — 2-column settings form row with 200 px label column + optional hint + single control slot + bottom hairline. Stacks label above control when `inline: false`. Taller and more opinionated than stable `FormRow`.
- feat: `SuffixInput` — `T.TextField` variant with an optional mono right-edge suffix (`MM`, `HU`, `NCM`, `WL HU`, `WW HU`) and a `mono: bool` switch for identifier-style fields (DICOM node, AE title, license number).
- feat: `StatusToggle` — 34×18 pill toggle with a 14×14 white thumb + mono uppercase status label (default `ON` / `OFF`; security section uses `Enabled` / `Disabled`).
- feat: `ReadoutSlider` — slider with a 72 px right-aligned mono readout column showing `value unit`. Precision auto: 1 decimal when `stepSize < 1`, integer otherwise. Used for safety thresholds, torque, cache, timeout, font scale.

### Added — Experimental: DALI settings (tranche B — visual atoms)

- feat: `ColorDot` — 22×22 circular color swatch with 2 px white inner border and 1 px outer ring. Emits `clicked()`.
- feat: `ThemeTile` — 120 px theme picker tile with a 70 px preview rendering a serif `Aa` glyph on the theme's background + mono label below. Active state adds 2 px blue border + 3 px outer glow.
- feat: `StorageBar` — 3-row disk usage meter: mono label + right-aligned percentage on top, 6 px progress bar in the middle, mono muted caption on the bottom. Auto-computes percentage from `used / total`.
- feat: `ConnectionStatus` — 8 px glowing dot + mono bold uppercase state label + mono muted caption. States: `online` / `offline` / `error`. Heavier than `SaveIndicator`.

### Added — Experimental: DALI settings (tranche C — layout primitives)

- feat: `SettingsHeader` — page-level section header: mono eyebrow + 28 px serif title + 13 px muted description capped at 640 px. Smaller sibling of `EditorialHero`.
- feat: `Group` — sub-section container: mono uppercase title + 1 px bottom border + default-property content slot + 32 px outer bottom margin.
- feat: `NumberedNavItem` — 2-column sidebar nav row: 28 px mono index column + title + subtitle. Active state tints the tile blue (`#EFF6FF` / `#BFDBFE`).

### Added — Experimental: DALI settings (tranche D — compound shells)

- feat: `AvatarStack` — horizontal row of overlapping 34×34 initials avatars with an optional trailing `+` add button. Accepts an array of `{ initials, background, foreground }`.
- feat: `PlanCard` — tinted horizontal billing card: mono eyebrow + serif title + vertical divider + mono price (with `/ mo` suffix) + outline `UPGRADE` chip. Hardcoded `#EFF6FF` / `#BFDBFE` surface.
- feat: `SignaturePreview` — 180×54 framed cursive signature preview + outline mono `REDRAW` button. Emits `redrawRequested()` on redraw click.
- feat: `AuditLogRow` — 3-column audit log entry: mono time (80 px) + mono muted date (90 px) + severity dot (`ok` / `info` / `warn`) + event text. 1 px bottom hairline.
- feat: `ProfileHeader` — compound: 96×96 gradient avatar with serif initials + corner `+` overlay + vertical block of serif name, muted title, mono 3-slot stats strip (`LICENSE / SINCE / PLANS`).

### Added

- feat: clinical primitives for DALI launch flow — `StatusPill` (ready/processing/reviewed/neutral tonal pill with dot), `SectionLabel` (mono uppercase label), `MetaRow` (horizontal key/value display row), `ProgressLine` (3px linear-gradient progress with 55ms tick), `SearchField` (T.TextField with built-in search glyph + ⌘K hint pill).
- feat: clinical composites — `BrandTile` (gradient tile with tooth glyph), `FilterRow` (selectable list row with 2px state strip + count pill), `InfoCard` (tinted info/warning/success/neutral card), `SystemInfoGrid` (4-col label+value footer with optional status dot), `TopMetaStrip` (hairline meta strip with left/right slots), `ScannerMark` (animated splash mark: rotating dashed ring + solid ring + sweeping arc + white center tile with tooth glyph), `Thumb` (CBCT slice placeholder tile — axial/coronal/sagittal/pano/3d with crosshair), `Render3D` (volumetric placeholder with implant marker + canal hint).
- feat: `internal/PearlToothIcon` — dental tooth glyph atom used by BrandTile and ScannerMark.
- example: `examples/splash_library.py` + `App.qml` / `Splash.qml` / `CaseLibrary.qml` — full DALI launch flow (splash → case library) rendered entirely from pearl-kit components. Fonts (DM Serif Display / Plus Jakarta Sans / JetBrains Mono) bundled under `examples/fonts/` and loaded via `QFontDatabase.addApplicationFont` at startup.
- docs: live WebAssembly component previews embedded in every docs page via Qt for WebAssembly. One shared `pearl_kit_gallery.{wasm,js,html}` bundle built in CI (`docs.yml`) using Qt 6.8 wasm_singlethread + emsdk 3.1.56, routed into each component page through an `?demo=<slug>` URL parameter. Bundle is fetched locally with `./scripts/fetch-wasm.sh` (no toolchain required, ~15 MB) or rebuilt with `./scripts/build-wasm.sh`. `Tokens.qml` typography (SF Pro Display / SF Mono) is substituted to Inter / JetBrains Mono in the WASM runner via `QFont::insertSubstitution` — the Python package is untouched. Detach action in the DetachableTabView demo is disabled with a hint because secondary OS windows do not render inside an iframe; CodeBlock clipboard copy silently no-ops for the same Emscripten reason.
- `PearlKit.ListView` — generic vertical list primitive with shadcn-styled chrome for use-cases like implant lists, measurements, recent files, and log entries. shadcn/ui ships no pure ListView primitive; design values blend `Command.Item` (selected / hover `Tokens.accent` fill, `Tokens.radius.sm` rounding, 8 px horizontal + 6 px vertical padding, 16 px icons, 12 px muted trailing text), `Table.Row` (opt-in 1 px `Tokens.border` bottom hairline via `showSeparators`), and `Command` root (`Tokens.radius.md` container with 1 px `Tokens.border` on `Tokens.card`). Built on `QtQuick.Templates.Control` with its `contentItem` set to a native `QtQuick.ListView` — the outer `T.Control` exists only for `focusPolicy` + `visualFocus` (keyboard-only 3 px `ring/50%` focus ring wrapping the container, shadcn `focus-visible:` parity), while the inner ListView keeps every Flickable affordance (delegate recycling, `keyNavigationEnabled`, `boundsBehavior: StopAtBounds`, header / footer slots, `currentIndex`). Two variants: `"card"` (default, 1 px border + `Tokens.radius.md` + `Tokens.card` background — standalone list panels) and `"flush"` (no chrome — for lists nested inside `Card` / `GroupBox` where an outer bordered parent already exists). Default delegate is a `T.ItemDelegate` (same base as Select's popup rows) resolving `label` / `iconSource` / `trailing` / `enabled` roles from both `model` (ListModel / object models) and `modelData` (primitive-array models) with primitive-string fallback. Role lookup pattern mirrors `Select.qml` exactly; per-row `enabled: false` fades background + text to 50 % alpha and gates `hovered` / `clicked`. Hover and selected collapse into a single `Tokens.accent` fill (pearl-kit precedent from Select / Menu), transitioning at `Tokens.motion.fast` (150 ms) `Easing.OutCubic`. Vertical scrollbar is an inline `T.ScrollBar` identical to `ScrollArea` (10 px width, 8 px inner radius, `Tokens.border` idle → `mutedForeground @ 80 %` hover → full `mutedForeground` pressed, 180 ms autoHide fade on hover) — inline `HoverHandler` drives the fade gate. `emptyText: string` renders centered muted placeholder when `count === 0`. Properties: `variant`, `showSeparators`, `emptyText`, `rowHeight` (default 36 px), plus aliases to `model`, `delegate`, `currentIndex`, `count`, `spacing`, `orientation`, `header`, `footer`. Signals: `itemClicked(int index)` on single left click, `itemActivated(int index)` on double-click or Return / Enter key. Keyboard: Up / Down / Home / End / PageUp / PageDown natively via ListView's `keyNavigationEnabled: true`; Return / Enter handled by an explicit `Keys.onReturnPressed` / `Keys.onEnterPressed` on the inner ListView (allowed — the "no manual Keys handlers" rule applies to `T.Button` / `T.TextField`, neither of which ListView extends). Disabled state fades `background` + ListView body to 50 % alpha without touching the root — standard pearl-kit rule, keeps the focus ring crisp. Individual rows have `focusPolicy: Qt.NoFocus` so Tab does not cycle through every row. `currentIndex` defaults to `-1` (no initial selection); consumers set `0` for initial highlight. Deliberate divergences: no direct shadcn equivalent (blends three primitives into one); opt-in separators via `showSeparators` rather than Table's always-on `border-b`; single-line rows only in default delegate (two-line `description` deferred — custom delegate override handles the need); `"card"` / `"flush"` variant instead of Tailwind responsive class system (desktop-first); single-selection only (multi-selection, Ctrl / Cmd+Click, Shift+Click range all deferred until a concrete consumer ask); no drag-to-reorder; no `CommandGroup`-style section headers (Select's `type: "header"` role already covers that use case).
- `PearlKit.CodeBlock` — monospace fenced-code surface for chat-message rendering and any other `<pre><code>` use case. Composite `Item` (no `QtQuick.Templates` base — `CodeBlock` has no single interactive contract; the copy button carries the Tab focus by itself) with a `Tokens.card` rounded surface at `Tokens.radius.md`, a 1 px `Tokens.border` outline, and `clip: true` so the optional header's muted fill does not bleed past the rounded corners. Public API: `code: string`, `language: string` (informational — see divergence below), `filename: string`, `showCopyButton: bool` (default `true`), `copyTimeout: int` (default `2000` ms), `maxBodyHeight: int` (default `360`), readonly `copied: bool`, `copyRequested()` signal, and an imperative `copyToClipboard()` method so external triggers can drive the same copy flow. The header is a 44 px row at `Tokens.muted` (a deliberate remap from kibo's `bg-secondary` — pearl-kit's `Tokens.secondary` is the purple accent used by `Button.variant: "secondary"`, not a neutral backdrop); the filename renders as `PearlText` at `Tokens.font.mono` / `Tokens.font.size.xs` / `Tokens.mutedForeground` with `elide: Text.ElideRight`; a 1 px `Tokens.border` hairline separates header from body. The copy button is a local `T.Button` (not the public `PearlKit.Button`) because the glyph swaps between a new `PearlCopyIcon` Canvas atom and the existing `PearlCheckIcon` on copy success, and the public Button only accepts a URL-based `iconLeft` / `iconRight` — routing that through a custom `contentItem` requires `T.Button` directly. Ghost-styled: `Tokens.accent` hover fill, `Tokens.accentForeground` icon tint on hover, `Tokens.success` tint on the check glyph for the copied state, 3 px `PearlFocusRing` at `ring @ 50 %` keyboard-only via `visualFocus` (Button parity). Body is a `Flickable` (not pearl-kit's public `ScrollArea`, because `T.ScrollView` silently reparents direct children into its internal `Flickable.contentItem`, which breaks reading `TextEdit.contentWidth` / `contentHeight` from the outside) wrapping a read-only `TextEdit` — `readOnly: true`, `selectByMouse: true`, `wrapMode: TextEdit.NoWrap`, `selectionColor` at `ring @ 35 %`, `renderType: TextEdit.NativeRendering`. The `Flickable.contentWidth` / `contentHeight` are bound to `_code.contentWidth` / `contentHeight` plus 16 px per side, which drives horizontal and vertical scrollbars automatically; `maxBodyHeight` caps the `Layout.preferredHeight` so long files do not blow out the parent layout. Clipboard copy runs through a hidden zero-sized `TextEdit` (`_clipboardHelper.text = code; selectAll(); copy(); deselect()`) — pure-QML pattern, no Python-side `QGuiApplication.clipboard()` plumbing, works on macOS / Windows / Linux identically. A `Timer` at `copyTimeout` ms flips `_copied` back to `false` after a successful copy so the check glyph auto-reverts. Disabled state fades the `_surface` `Rectangle` to 50 % alpha without touching the root control (standard pearl-kit rule — keeps the copy button's focus ring crisp). Deliberate divergences from kibo / shadcn.io CodeBlock: no Shiki syntax highlighting (web-only library, no Qt equivalent — pearl-kit's core scope renders plain monospace foreground, reserving the `language` prop for a future `QSyntaxHighlighter`-based `Full` scope); no multi-file `data: CodeBlockData[]` with a language-selector dropdown (single-file only — consumers who need multi-file switching compose two `CodeBlock` instances inside a `TabBar` / `StackedView` pair); no line numbers, diff markers, or line focus (Shiki transformers without Shiki have no surface to bind to); no 80+ filename-icon auto-detection (simple-icons glyph set would require matching Canvas atoms — deferred); `Tokens.card` surface + `Tokens.muted` header instead of kibo's `bg-transparent` + `bg-secondary` (reads as an elevated surface on a plain page without extra composition); header is 44 px hardcoded (4 + 36 + 4 kibo math) but filename's kibo-specific `px-4 py-1.5` nested padding is replaced by a single 16 px `anchors.leftMargin` for clarity.
- `PearlKit.internal.PearlCopyIcon` — Canvas-based stroked copy glyph following the `PearlXIcon` / `PearlCheckIcon` / `PearlChevron` pattern. Two shapes normalized from lucide `Copy` (viewBox 24 × 24): a rounded 14 × 14 front square at `(8, 8)` and an L-shape from `(16, 4)` to `(4, 4)` to `(4, 16)` forming the back document. `strokeColor: color` (default `Tokens.mutedForeground`) and `strokeWidth: real` (default `1.4`) properties with repaint hooks on `width` / `height` changes so the glyph stays sharp when scaled. Avoids shipping a copy SVG asset or depending on lucide.
- `PearlKit.Badge` — small pill-shaped label port of shadcn/ui v4 Badge. Six variants (`default`, `secondary`, `destructive`, `outline`, `ghost`, `link`) at a single size matching shadcn exactly (12 px `Tokens.font.size.xs` medium text, 8 × 2 px padding, `Tokens.radius.full` pill shape, 1 px baseline border that flips color on the outline variant). Root is a plain `Item` — no `QtQuick.Templates` base because Badge has no interactive contract (no hover, no pressed, no focus, no disabled semantics beyond opacity fade). Pearl-kit extension: `iconLeft` / `iconRight` URL slots at 12 × 12 (shadcn `[&>svg]:size-3`) rendered inside a centered `Row` with 4 px `gap-1` spacing, matching Button / Input / NavItem slot conventions. Text uses a plain `Text` element (not `PearlBaseText`) because the 12 px / variant-colored / optionally-underlined typography does not reuse any `PearlBaseText` default; `renderType: Text.NativeRendering` + `antialiasing: true` for crisp Retina rendering. Background `Rectangle` has `clip: true` mirroring shadcn's `overflow-hidden`, and `Behavior on color` at `Tokens.motion.fast` (150 ms `Easing.OutCubic`) animates variant / theme color changes smoothly. `destructive` foreground hardcoded to `"#FFFFFF"` (shadcn uses `text-white` literal rather than routing through `destructive-foreground`, preserving identical visual identity in Light mode). Implicit height floored at 20 px — shadcn's `py-0.5` + 12 px text renders at ~20 px in Chrome, so the floor prevents visual collapse on macOS. Disabled state fades background and content `Row` to 50 % alpha separately; root `Item` opacity untouched (standard pearl-kit rule). Accessibility via `Accessible.role: Accessible.StaticText` with `Accessible.name: text` — closest Qt semantic match for a non-focusable labeled element. Deliberate divergences from shadcn: no hover state (shadcn's `[a&]:hover:bg-primary/90` only applies when rendered as an anchor via `asChild`; pearl-kit Badge is static `<span>` equivalent with no anchor mode — consumers wrap Badge in `MouseArea` or use a `Button { size: "xs" }` for click feedback); link variant always underlines text (shadcn's `[a&]:hover:underline` only shows on anchor hover; pearl-kit underlines unconditionally since there is no interactive mode); `iconLeft` / `iconRight` as URL slots instead of shadcn's child-composition with `[&>svg]:size-3` descendant selector (pearl-kit matches Button / Input / NavItem API); no size variants (shadcn has one size); no `focus-visible:` ring or `aria-invalid:` styling (non-focusable, non-form element); no counter / notification dot affordance (deferred until a real consumer asks).
- `PearlKit.DetachableTabView` / `PearlKit.DetachableTab` / `PearlKit.FloatingWindow` — pop-out tab orchestration ported from airontgen's `ui/detachable_tab_widget.py`. `DetachableTabView` is a plain `Item` root (no `QtQuick.Templates` base — tab orchestration has no interactive contract of its own) wrapping a `ColumnLayout` of `TabBar` + host `Item`; user-declared `DetachableTab` children land in the host via `default property alias _tabsContent: _host.data`. Detach triggers on `TabButton.doubleClicked` (native `T.AbstractButton` signal — no custom `Keys.on*` handlers), reparents the whole `DetachableTab` Item (and therefore its entire child subtree) into a freshly-created `FloatingWindow`, so all running timers / scroll positions / selection state survive pop-out and re-dock. Detach / dock dict-keyed on an auto-generated counter (not `stackKey`) so duplicate-keyed or empty-keyed tabs can still detach independently. `FloatingWindow` is a frameless top-level `Window` (`Qt.Window | Qt.FramelessWindowHint`) with a 32 px custom titlebar — drag area with global mouse-delta window move, double-click to toggle `Window.Maximized` / `Window.Windowed`, three 24 × 24 chrome buttons (dock / maximize / close — using new `PearlKit.internal.PearlDockIcon` + `PearlMaximizeIcon` Canvas atoms plus existing `PearlXIcon`, hover backgrounds at `Tokens.destructive` for close and `foreground @ 8%` for the others transitioning at `Tokens.motion.fast`), and a 16 × 16 bottom-right resize grip via `Qt.SizeFDiagCursor` (hidden while maximized). `onClosing: close.accepted = false; dockRequested()` intercepts every native close attempt — a detached tab can never be lost, only rehomed (DALI parity: close = dock). `FloatingWindow` auto-sizes to ~70 % of the host Window's dimensions at detach time, offset +50 / +50, and uses `Window.window` attached property to find the host plus `transientParent` to inherit modality / always-on-top semantics on macOS. Declarative API: `DetachableTab { title; stackKey; iconSource; permanent; nonDetachable }` with priority semantics — `permanent` tabs cannot detach and (if ever rehomed through other paths) re-dock at index 0; `nonDetachable` tabs simply cannot detach. `currentKey` / `currentIndex` two-way sync follows the `StackedView` pattern with a `_ready` gate in `Component.onCompleted` to prevent initialization-time feedback loops, and uses a `_dockedModel: var` array (rebuilt manually on detach / dock via `_rebuildModel()`) that the internal `Repeater` binds against so `T.TabBar`'s children re-populate deterministically. Public methods: `detachTab(index)`, `detachTabByKey(key)`, `dockTab(tab)`, `dockTabByKey(key)`, `raiseFloating(tab)`, `closeAllFloating()`, `tabAtIndex(index)`, `tabByKey(key)`, `indexOfKey(key)`. Public signals: `tabDetached(Item, string)` / `tabRedocked(Item, string)`. Readonly introspection: `floatingCount: int`, `dockedCount: int`. Deliberate divergences from DALI's widget: drag-to-detach, right-click context menu, and per-tab ↗ detach icon deferred — pearl-kit v1 supports only the double-click affordance to keep the detach trigger surface minimal; `replace_tab_widget` API omitted (consumers reassign `DetachableTab` children directly); no `TabPlaceholder` sleep-manager integration (DALI-specific dependency). Deliberate divergence from shadcn: no shadcn equivalent exists — documented as a pearl-kit original for the DALI viewer / editor call sites.
- `PearlKit.internal.PearlDockIcon` — Canvas-based stroked icon atom rendering a downward arrow dropping into a three-sided tray (shadcn-adjacent "dock" glyph). Follows the `PearlXIcon` / `PearlChevron` pattern — `strokeColor` / `strokeWidth` props, `onWidthChanged` / `onHeightChanged` repaint, no SVG asset, no `QtSvg` plugin dependency. Used exclusively by `FloatingWindow`'s dock chrome button but shipped as an internal atom so custom titlebar implementations can reuse it.
- `PearlKit.internal.PearlMaximizeIcon` — Canvas-based stroked icon atom with a `restored: bool` toggle — `false` paints a single rounded square (maximize action), `true` paints two offset overlapping squares (restore-from-maximized action). Same `PearlXIcon` pattern (`strokeColor` / `strokeWidth` props, repaint on dimension / state change). Used by `FloatingWindow`'s maximize chrome button.
- `PearlKit.TextArea` — multiline text input port of shadcn/ui v4 Textarea. Auto-grows from a 64 px floor (`minHeight` property, shadcn `min-h-16`) via `implicitHeight: Math.max(minHeight, contentHeight + topPadding + bottomPadding)` — pearl-kit's port of CSS `field-sizing-content`. `property bool error` flips border + focus ring to destructive (shadcn `aria-invalid:border-destructive` + `aria-invalid:ring-destructive/20`). `property bool mono` swaps font family from `Tokens.font.ui` (SF Pro Display) to `Tokens.font.mono` (SF Mono) — a pearl-kit extension beyond shadcn for license key entry, log viewers, and CLI output capture, without requiring consumers to compose two primitives. 12 / 8 px padding (shadcn `px-3 py-2`), `Tokens.radius.md` corners, theme-aware background (transparent in Light, `Tokens.input @ 30 %` in Dark variants), `wrapMode: TextEdit.Wrap` default. Built on `QtQuick.Templates.TextArea` with the default `TextEdit` `contentItem` preserved intact (native caret / IME composition / multi-line selection / undo-redo / click-to-position — overriding would silently lose all of it). Focus ring 3 px @ `ring / 50 %` (destructive-tinted at 20 % when `error: true`), `offset: 0`, visible on any focus source via `activeFocus` — matches Input, because text inputs need caret feedback on mouse click too (shadcn `focus-visible:` is keyboard-only; pearl-kit deliberately diverges for text-input UX parity with Material / Mantine / Chakra / Radix). Disabled state fades `background` opacity + text / placeholder color ternaries independently; the root control never gets `opacity: 0.5` so the focus ring stays crisp (standard pearl-kit rule). `readOnly` passes through for log-viewer call sites — selection / copy / caret navigation stay available, typing is suppressed. Deliberate divergences from shadcn: `mono` variant (no shadcn equivalent — driven by DALI license-key and log-viewer call sites); uniform 14 px `Tokens.font.size.sm` (shadcn responsive `text-base md:text-sm`, pearl-kit is desktop-first); `activeFocus` focus ring trigger (shadcn `focus-visible:` keyboard-only); `minHeight` exposed as an explicit property (shadcn bakes `min-h-16` into class list — pearl-kit lets consumers set tighter or roomier floors without subclassing). Unlike `T.TextField`, `T.TextArea` does not emit `accepted()` on Return — Return inserts a literal newline; consumers wire `Ctrl+Return` via a sibling `Shortcut` for submit semantics.
- `PearlKit.Avatar` — compact circular identity chip for chat UIs, contact lists, and user menus. Plain `Item` root (no `QtQuick.Templates` base — Avatar has no interactive contract) with a single child `Rectangle` whose `radius: width / 2` + `clip: true` produces the circle and crops overflow. Three size buckets (`sm` 24 px / `default` 32 px / `lg` 40 px) matching shadcn `size-6` / `size-8` / `size-10`; text falls back to 12 px on `sm` (shadcn `text-xs`) and 14 px otherwise (`text-sm`). Three `variant` values (`default` / `primary` / `secondary`) — pearl-kit extension over shadcn's uniform `bg-muted` fallback, mapping to `Tokens.muted` / `Tokens.primary` / `Tokens.secondary` for chat role indication (assistant vs user) in a single primitive. Content resolves through a priority chain: `source` (URL, renders `Image` with `fillMode: Image.PreserveAspectCrop` and `sourceSize: Qt.size(d * 2, d * 2)` for retina sharpness) → `iconSource` (URL, centered `Image` at size-dependent 14 / 16 / 20 px with `sourceSize` rasterization) → `initials` (centered `Text` at `Tokens.font.weight.medium`, theme-reactive color via `_v.fg`). Visibility-gated so only one content layer renders at a time — consumers can wire a single `Avatar` to a user object with optional profile picture without branching. Disabled state fades the background `Rectangle` opacity to 50 % via standard pearl-kit rule (root untouched). No hover / pressed / focus state — wrap in a `MouseArea` or `Button` for interactivity. Deliberate divergences from shadcn/ui: single-component API instead of six (`Avatar` / `AvatarImage` / `AvatarFallback` / `AvatarBadge` / `AvatarGroup` / `AvatarGroupCount` collapsed into one with priority-resolved slots); `variant` property added for role tinting; `iconSource` middle-priority slot added (shadcn has no icon variant); `AvatarBadge` presence dot and `AvatarGroup` overlapping stack deferred until a real consumer ask.
- `PearlKit.MenuBar`, `PearlKit.Menu`, `PearlKit.MenuItem`, `PearlKit.MenuSeparator` — desktop menu family for application menu bars and right-click context menus. Built on `QtQuick.Templates.MenuBar` / `.Menu` / `.MenuItem` / `.MenuSeparator`. `Menu` doubles as a menubar dropdown and a standalone context menu via `popup()` / `popup(x, y)`; submenus are declared natively by nesting `Menu` inside `Menu` (chevron auto-added via `control.subMenu !== null` check). `MenuBar` attaches to `ApplicationWindow.menuBar` for native macOS global menu bar placement, or renders inline as a regular `Item`; its `delegate` styles each `T.MenuBarItem` as a 36 px-tall trigger (`Tokens.accent` fill + `Tokens.accentForeground` text on hover / highlighted / `menu.opened`). `Menu` popup is a `T.Menu` (not `T.Popup`, because cascading submenu semantics require the menu-specific base): `Tokens.popover` background, `Tokens.radius.md` corners, 1 px `Tokens.border`, shadow via `QtQuick.Effects.MultiEffect` (Qt 6.5+, matching `Dialog`) with `shadowVerticalOffset: 4` + `shadowBlur: 1.0` + `shadowColor: Qt.rgba(0, 0, 0, 0.15)` (shadcn `shadow-md` parity) and a 16 px `paddingRect` expansion so the blur isn't clipped. Enter transition runs a 150 ms parallel fade (0 → 1) + scale (0.95 → 1) with `Easing.OutCubic` (shadcn `animate-in fade-in-0 zoom-in-95`); exit is fade-only. `minimumWidth: 192` matches shadcn `min-w-[12rem]`; `modal: false` + `cascade: true` are mandatory for submenu positioning and outside-click dismissal. `MenuItem` properties: `text`, `shortcut` (display-only string, right-aligned, `Tokens.font.size.xs` + `letterSpacing: 2.4` + `Tokens.mutedForeground`, kept muted even on hover per shadcn CSS specificity), `iconSource: url` (16 × 16 `Image` with `sourceSize: Qt.size(16, 16)`), `variant: "default" | "destructive"` (destructive: `Tokens.destructive` text, `Qt.rgba(destructive, 0.1)` hover bg, destructive icon tint), `checkable`, `checked`, `radio` (when `checkable && checked && radio`, renders a filled 8 × 8 dot via `Rectangle { radius: 9999 }` instead of the default `PearlCheckIcon`), `inset` (reserves the 16 px indicator zone for alignment with checkable siblings). Implicit height locked at 32 px (shadcn `py-1.5` = 6 + 6 + 20 text). `T.MenuItem.contentItem` is overridden with a `RowLayout` holding `[indicator? | icon? | text | shortcut? | submenu chevron?]`, which suppresses the native `indicator` and `arrow` slots — everything is composed inline. Mnemonics (Alt-underline) work via `textFormat: Text.StyledText` on both `MenuItem.contentItem` and the `MenuBarItem` delegate. `MenuSeparator` is a 1 px `Tokens.border` line with negative `anchors.leftMargin` / `rightMargin` on the inner `Rectangle` (not root padding — `T.MenuSeparator` rejects negative padding) so the hairline bleeds to the popup's inner edge (shadcn `-mx-1`). Keyboard navigation is native `T.Menu` — Alt + letter for mnemonics, Up / Down, Return / Space, Right / Left for submenu entry / exit, Escape to close, type-ahead. No focus ring on items or menubar triggers — the accent background already conveys highlighted state (shadcn `focus:bg-accent` convention). Disabled opacity applied to `background` and `contentItem` separately per pearl-kit's never-fade-the-root rule. Deliberate divergences from shadcn: `iconSource: url` direct prop instead of icon composition (Button / Input parity); `shortcut` is display-only (firing is the user's responsibility via `Shortcut {}` / `Action {}`); `radio: bool` modifier on `MenuItem` instead of separate `MenuRadioItem` / `MenuCheckboxItem` components (smaller surface, one component covers both patterns); popup shadow via `MultiEffect` instead of CSS `shadow-md`; `MenuBar` `shadow-xs` skipped (top bars sit flush against the window chrome without a hairline shadow). Out of scope for v0.4.0: `MenuLabel` / section header, `MenuGroup` / `MenuRadioGroup`, automatic `inset` propagation, `QKeySequence`-parsed shortcuts, `sideOffset` / `alignOffset` customization.
- `PearlKit.internal.PearlChevron` — added `"right"` and `"left"` direction branches (existing `"up"` / `"down"` unchanged). Enables the submenu-indicator chevron on `MenuItem` and is reusable for any future right-pointing affordance.
- `PearlKit.IconStrip` — generic 48 px vertical sidebar nav rail with top-anchored main items and bottom-pinned footer items, driven by a controlled `activeKey` string and a JavaScript-array model (`items` / `footerItems` each of shape `{ key, iconSource, label }`). Root is a plain `Rectangle` filled with `Tokens.muted` (no `QtQuick.Templates` base — the strip has no interactive contract); each tile is a `T.AbstractButton` with `hoverEnabled: true`, `focusPolicy: Qt.StrongFocus`, keyboard Space / Return activation, and `checked: _active` bound to `activeKey === key`. Active tile fills with `Tokens.accent`, hover fills with `Tokens.accent @ 60 %`, both transitioning at `Tokens.motion.fast` (150 ms `Easing.OutCubic`); an active state also reveals a 3 × 16 `Tokens.primary` indicator bar flush against the strip's left edge (airontgen parity — Apple HIG convention for vertical nav). Icon rendered as `Image` with `sourceSize: Qt.size(iconSize, iconSize)` at opacity 0.6 idle / 1.0 hover-or-active (Qt 6 has no raster color tint; pearl-kit dims opacity instead — consumer provides foreground-white SVGs). Optional label below icon at 11 px regular, color-reactive (`Tokens.mutedForeground` → `Tokens.foreground` on active); globally hideable via `showLabels: false` for an icon-only rail. Dimensional overrides: `stripWidth: 48`, `tileHeight: 52`, `iconSize: 20`. Layout via two anchored `Column` + `Repeater` pairs (top anchored to `parent.top`, footer anchored to `parent.bottom`) — no `Flickable` overhead, no `ColumnLayout` spacer. Focus ring 3 px @ `Tokens.ring / 50 %`, per-tile, keyboard-only via `visualFocus` (Button parity). Disabled state fades `background` + `contentItem` to 50 % alpha; root opacity untouched (standard pearl-kit rule). Deliberate divergences from shadcn/ui: no direct equivalent exists (closest analogs are `Sidebar` — too broad — and `Tabs` with `orientation="vertical"` — implies associated `TabsContent`). Model-driven API instead of declarative `IconStripItem` children (matches `Select`'s ComboBox-style model). `footerItems` as a parallel array instead of a `<footer>` slot (simpler, flat API surface). Opacity-based icon dimming instead of raster color tint (Qt 6 `ColorOverlay` deprecation). 3 px left-edge indicator bar instead of shadcn Tabs's bottom-border accent.
- `PearlKit.NavItem` — sidebar / nav-list row primitive. Single-row clickable item with `text`, `iconLeft`, `iconRight`, `size` (`"sm"` / `"default"` / `"lg"`), and consumer-managed `active: bool` for selected-state styling. Built on `QtQuick.Templates.Button` so `clicked()`, Space / Return activation, and Tab focus chain come from the base template; `T.ItemDelegate` was rejected because its built-in `highlighted` would shadow the explicit `active` prop. Shadcn `SidebarMenuButton` default-variant parity: 8 px horizontal + vertical padding (`p-2`), 8 px gap between icon-label-icon (`gap-2`), `Tokens.radius.md` corners (`rounded-md`), hover / pressed / active all fill with `Tokens.accent` + `Tokens.accentForeground` (shadcn cascades `hover:bg-sidebar-accent` and `data-[active=true]:bg-sidebar-accent` to the same token; collapsed into a single `_highlighted` ternary), and active additionally swaps the font weight to `medium` (shadcn's `data-[active=true]:font-medium`) without changing the color. Size table: sm = 28 × text-xs (12 px), default = 32 × text-sm (14 px), lg = 48 × text-sm — matches shadcn `h-7` / `h-8` / `h-12`. Label uses `PearlBaseText` with `elide: Text.ElideRight` and an explicit `width` bound to `control.availableWidth` minus the icon slot widths so long labels truncate cleanly when a 13-tab sidebar is width-constrained, instead of pushing the container wider. 16 × 16 icons (shadcn `size-4`) with `sourceSize: Qt.size(16, 16)` for crisp SVG rasterization. Disabled fades `background` + `contentItem` to 50 % alpha without touching the root control, so the focus ring stays full-alpha per pearl-kit convention. Focus ring 3 px @ `ring / 50 %`, `offset: 0`, keyboard-only via `visualFocus` (button-like, matches shadcn `focus-visible:ring-2`; clicking does not leave a ring). Consumers compose a sidebar via `ColumnLayout { spacing: Tokens.space.x1; NavItem { Layout.fillWidth: true; ... } }` and own the `currentTab` state — no separate `NavList` container ship (kept as a "kit, not framework" call; pairs naturally with `StackedView` for pane switching). Deliberate divergences from shadcn / ui: no `variant: outline` (the `outline` variant in `sidebar.tsx` is used for decorative group-label buttons with a 1 px shadow ring — out of scope for a plain nav row); no `tooltip` prop (shadcn shows a tooltip when the parent sidebar collapses to icon-only mode — pearl-kit has no collapsible-sidebar primitive yet, so this is deferred); no `SidebarMenuAction` / `SidebarMenuBadge` / `SidebarMenuSub` (separate primitives in shadcn — defer until a real consumer ask); `iconRight` is a URL slot (shadcn composes external SVGs as children; pearl-kit matches `Button` / `Input` slot convention for consistency across the kit); no `ButtonGroup` auto-wiring for exclusive selection (intentional — 13-tab sidebars use a single `currentTab` index and `onClicked: currentTab = index`, which is simpler than attached-property plumbing and matches settings-dialog idioms).
- `PearlKit.TabBar` / `PearlKit.TabButton` — two-component tab strip port of shadcn/ui v4 Tabs. `TabBar` wraps `QtQuick.Templates.TabBar` with two variants: `default` (shadcn `bg-muted` 8 px pill at 36 px height with 3 px inner padding and 0 px gap between triggers) and `line` (document-mode: transparent strip with 1 px `Tokens.border` bottom hairline and 4 px gap). `TabButton` wraps `QtQuick.Templates.TabButton` with full visual override — idle text at `Tokens.foreground @ 60%`, hover/active at 100%, active default variant fills with `Tokens.background` plus a `shadow-sm`-approximation hairline, active line variant shows a 2 px underline at `Tokens.foreground` flush with the bottom hairline. Extensions beyond shadcn: `expanding: bool` on `TabBar` (default `true` for shadcn parity, `false` sizes each trigger to its content via a `Binding on width` that overrides T.TabBar's default fillItems behavior); `closable: bool` + `closeRequested()` signal on `TabButton` (12 × 12 trailing × rendered via the internal `PearlXIcon` Canvas atom, intercepted by a `MouseArea` with `preventStealing: true` + `ev.accepted = true` so clicking × never fires a tab switch); `iconSource: url` slot at 16 × 16 with `sourceSize` rasterization. Focus ring is the standard 3 px `PearlFocusRing` at `ring@50%`, `offset: 0`, keyboard-only via `visualFocus` (button-like convention). Disabled state fades `background` + `contentItem.opacity` independently, never the root (pearl-kit rule — keeps the focus ring crisp). Keyboard nav (Left / Right / Home / End) is native T.TabBar, no custom `Keys.on*` handlers. Deliberate divergences from shadcn: two public components instead of four (Qt's T.TabBar self-manages `currentIndex`, consumer uses `StackLayout { currentIndex: tabBar.currentIndex }` for content switching — no `Tabs` root or `TabsContent` wrapper); `closable` + `expanding` are pearl-kit extensions driven by DALI's ~5 primary editor-style call sites; line-variant underline sits flush at the bar's bottom rather than shadcn's `bottom-[-5px]` overhang (pearl-kit's 36 px strip has no CSS overflow context, flush placement reads cleaner); horizontal-only (vertical orientation and drag-to-reorder deferred).
- `PearlKit.StackedView` — content host primitive paired with Sidebar / Tabs / any external trigger for switching between panes without unmounting. Pearl-kit-specific (no shadcn equivalent — shadcn composes pane content inside a `Tabs` root with implicit context; pearl-kit splits the trigger and pane host so any trigger can drive any host). Subclasses `QtQuick.Layouts.StackLayout` and adds three extensions: `animated: bool` (default `false`) gates an opt-in 150 ms opacity fade-in on the incoming pane at `Tokens.motion.fast` with `Easing.OutCubic` (only the incoming pane animates — outgoing snaps to `visible: false` per StackLayout's visibility semantics, mirroring shadcn Tabs `data-[state=active]:animate-in` + `data-[state=inactive]:hidden`); `currentKey: string` (default `""`) provides named pane switching that two-way-syncs with `currentIndex` — assigning either updates the other, reading `currentKey` always reflects the visible child's `stackKey`; `indexOfKey(key)` helper returns the index of the first child whose `stackKey` matches, or `-1`. Each pane participates in key routing via an optional `property string stackKey` declared on the child; un-keyed children still work but can only be reached by index. Mixed keyed / un-keyed pane sets are allowed. Unknown keys are soft no-ops — assignment takes briefly then the initial-sync step snaps `currentKey` back to the actual visible child's key, so readers can detect misses by reading back. State machine uses a private `_ready` flag set during `Component.onCompleted` to gate both change handlers — without the gate, `StackLayout`'s internal `currentIndex` initialization fires `onCurrentIndexChanged` during construction and overwrites consumer-supplied initial `currentKey` before children are parsed; the guarded construction order first resolves `currentKey → currentIndex` (if the consumer set a key), then flips `_ready = true`, then calls `_syncKeyFromIndex()` so the initial `currentKey` is always authoritative from the visible child. Crossfade uses a single shared `NumberAnimation` whose `target` is reassigned on each index change (cheaper than one animation per child). Pure content host — no background, no border, no padding (pair with `Card` or a plain `Rectangle` for visible bounds). Children participate in `StackLayout` layout contracts via the normal `Layout.*` attached properties; non-current panes are hidden via `visible: false` and contribute nothing to layout. Deliberate divergences: no shadcn parity (pearl-kit splits trigger and host into independent primitives); `currentKey` extension for string-based routing; fade-only transition (no slide, no scale, no direction-aware animation — bi-directional slide would require tracking switch direction and breaks for wrap-around / non-adjacent jumps).

## [0.3.0] — 2026-04-18

### Added

- `PearlKit.Slider` — single-thumb continuous value selector with horizontal + vertical orientation and optional tick marks. Built on `QtQuick.Templates.Slider` with no `contentItem` override (T.Slider does not use one) and two custom slots: `background` holds the 6 px `rounded-full` track (`Tokens.muted`), a nested range `Rectangle` bound to `control.position` (`Tokens.primary`), and an opt-in tick `Repeater`; `handle` is a 16 × 16 `#FFFFFF` circle with a 1 px `Tokens.primary` border. Orientation-aware bindings swap the thin / long axes manually (T.Slider does **not** swap geometry on orientation flip). Horizontal defaults to 200 × 20, vertical to 20 × 176 (shadcn `min-h-44`). Vertical fill is **bottom-to-top** — lower values at the bottom of the track — matching iOS / macOS volume sliders and natural expectations for medical imaging window / level scales. Ticks are opt-in via `showTicks: bool` with `tickCount: int` (0 = auto from `stepSize`, capped at 21), rendered 1 × 4 px in `Tokens.mutedForeground` on the far side of the track. Thumb scales to 1.05× on `control.pressed` via `Behavior on scale` (`Tokens.motion.fast`, `Easing.OutCubic`). Keyboard navigation is native T.Slider (Left / Right / Up / Down / PageUp / PageDown / Home / End — no custom `Keys.on*Pressed` handlers). Focus ring 4 px @ `ring / 50 %`, `offset: 0`, keyboard-only via `visualFocus`, targeting `control.handle` so the ring follows the thumb. Disabled state fades track + handle to 50 % alpha without touching the root control (standard pearl-kit rule — keeps the focus ring crisp). Deliberate divergences from shadcn: no hover ring on the thumb (shadcn shows `ring-4` on both hover and focus-visible; 16 px thumb with constant pointer motion created visual noise — Button / Toggle convention wins); pearl-kit-specific `showTicks` + `tickCount` extension (shadcn Slider ships no ticks) for window / level and zoom controls; single-thumb only (two-thumb range selection would require `T.RangeSlider` and is deferred); no thumb `shadow-sm` (Toggle precedent — negligible on a control this small).
- `PearlKit.Toaster` / `PearlKit.ToasterHost` / `PearlKit.Toast` — Sonner-style notification system. `Toaster` is a `pragma Singleton` QML singleton exposing `show({type, title, description, duration})` (returns an integer `id`), `dismiss(id)`, and `dismissAll()`, plus configurable `position` (six variants, default `top-right`), `maxStack` (default `3`), and `defaultDuration` (default `4000` ms). `ToasterHost` is the renderer piece — a plain `Item` placed once inside `ApplicationWindow` that auto-reparents to `T.Overlay.overlay`, subscribes to `Toaster._stack`, and renders each row via a `Column` + `Repeater` with `add` / `move` transitions for stack animation. `Toast` is the delegate surface: `Item` + `Rectangle` (intentionally not `T.Popup` — stacked popups create z-order and modal conflicts), 356 px wide, `Tokens.popover` / `Tokens.popoverForeground` / `Tokens.border` / `Tokens.radius.md` mapped from Sonner's CSS variable exports, two-layer shadow via `MultiEffect`, type-colored icon (`PearlToastIcon` for success / info / warning / error, public `Spinner` reused for loading), `PearlBaseText` title (semibold) + description (regular), and a close button that mirrors `Dialog`'s close pattern (2 px `PearlFocusRing` @ `ring / 50 %`, Space / Return activation). Enter animation: 40 px slide + fade in at `Tokens.motion.base` (180 ms, `Easing.OutCubic`); exit is two-phase so adjacent toasts don't jump while the exiting row animates out. Hover pauses the auto-dismiss `Timer`; loading toasts default `duration: 0` and must be dismissed explicitly. Ships with `pearl_kit.NotificationCenter` + opt-in `pearl_kit.register_notifications(engine, icon_path=None)` — a Python `QObject` that wraps `QSystemTrayIcon` and exposes `isAppActive` (reactive via `applicationStateChanged`) + `showOsNotification(title, body, is_error)`; registered as a `NotificationCenter` context property so `ToasterHost` can probe for it at load time and route show calls to the OS tray when the app is backgrounded (replaces `airontgen`'s `NotificationManager` with the same focus-gated split). Deliberate divergences from Sonner: no swipe-to-dismiss, no action buttons, no rich content slot, no `promise()` API, hover-resume restarts the timer from zero instead of preserving remaining time. New `PearlKit.internal.PearlToastIcon` atom — 20 × 20 Canvas glyph dispatcher for success / info / warning / error (loading reuses public `Spinner`), following the `PearlChevron` / `PearlXIcon` / `PearlCheckIcon` pattern (no SVG assets, no `QtSvg` plugin dependency).
- `PearlKit.Tooltip` — seventeenth public component. Small transient hover overlay built on `QtQuick.Templates.ToolTip` with shadcn/ui v4 TooltipContent styling: inverted surface (`bg-foreground` → `Tokens.foreground`), inverted text (`text-background` → `Tokens.background`), `Tokens.radius.md` (6 px), 12 × 6 px padding, 12 px `text-xs` via `Tokens.font.size.xs`. Properties: `placement: "top" | "bottom" | "left" | "right"` (default `"top"`, no auto-flip — consumer picks the right side per trigger), `maxWidth: int` (default 320; long text wraps at this column via `Text.WordWrap` in concert with `implicitWidth: Math.min(implicitContentWidth + leftPadding + rightPadding, maxWidth)` and `contentItem.width: control.availableWidth`), `delay: 0` default matching shadcn/Radix `TooltipProvider` parity, `timeout: -1` (no auto-hide — desktop convention). `contentItem` overridden with `PearlBaseText` so font family / weight / `Text.NativeRendering` match the rest of pearl-kit typography; `background` overridden with a plain `Rectangle` at `Tokens.foreground` + `Tokens.radius.md`. Enter / exit run parallel `ParallelAnimation` fade (0 → 1) + scale (0.95 → 1) over `Tokens.motion.fast` (150 ms, Tailwind `animate-in` parity) with `Easing.OutCubic`. Positioning is explicit `x` / `y` computed from `parent.width` / `parent.height` per placement, flush against the trigger edge (shadcn `sideOffset: 0`). `closePolicy: T.Popup.NoAutoClose` so the tooltip only closes when the `visible` binding flips, never on outside click or Escape. `Accessible.role: ToolTip` with `Accessible.name` bound to `text` for VoiceOver / screen-reader support. No focus ring — tooltips don't receive focus (`focus: false`, the Popup default). Consumer wires visibility explicitly via `visible: trigger.hovered && trigger.enabled` — pure-QML idiom, no `QQuickStyle` attached-property plumbing. Deliberate divergences from shadcn: no arrow (macOS native convention, non-deterministic without auto-flip — deferred); no auto-flip placement (fixed 4-direction, consumer picks side — `FloatingUI`-style flip deferred to v0.4.0+); no enter-animation slide-in-from-opposite (visual delta imperceptible at 150 ms, keeps enter transition single-form across placements); no `sideOffset` property (hardcoded flush = shadcn source default; consumers override `x` / `y` for custom gap); no Qt attached `ToolTip.text` syntax (requires `QQuickStyle` C++ plugin — pearl-kit stays pure-QML, standalone `P.Tooltip { }` is the only entry point); no `TooltipProvider` / portal / state-machine layers (QML-idiomatic: one standalone popup per trigger, visibility consumer-wired).
- `PearlKit.MessageBox` — sixteenth public component. Opinionated modal-message primitive built as a QML subclass of `PearlKit.Dialog`, with four `variant` values (`info` / `warning` / `error` / `confirm`) driving both an auto-generated tinted icon badge and the footer button set. Properties: `variant: string`, `heading: string`, `message: string`, `okText: string` (default `"OK"`), `cancelText: string` (default `"Cancel"`), `okVariant: string` (defaults to `"destructive"` when `variant === "error"`, else `"default"`; consumer-overridable). Signals: `accepted()` (OK clicked / Enter on focused OK) and `rejected()` (Cancel clicked, or Escape / overlay dismiss for `confirm` variant only). MessageBox suppresses Dialog's native header (`title: ""`, `description: ""`) and renders its own `icon + heading + message` `ColumnLayout` inside the inherited `_bodyContent` default slot; the footer is auto-built as a right-aligned `Row` with a Button (`okVariant`) always present and an outline Cancel Button visible only when `variant === "confirm"`. `showCloseButton: false` and `maxWidth: 420` hardcoded. For the `confirm` variant, `closePolicy` downgrades to `T.Popup.CloseOnEscape` only (overlay-click dismiss blocked, PyQt6 `QMessageBox` idiom) and an internal `QtObject { property bool resolved }` flag tracks button-driven vs. Escape-driven close so `onClosed` emits `rejected()` iff the user Escaped without picking. `initialFocusItem: okButton` auto-focuses OK on open so Enter immediately accepts. Deliberate divergences from shadcn's AlertDialog (12 primitives collapsed to one component, 4-variant taxonomy extending shadcn's `default`/`destructive`, variant-tinted badge at 15% alpha vs. shadcn's neutral `bg-muted` `AlertDialogMedia`, auto-generated footer instead of opt-in `AlertDialogAction` / `AlertDialogCancel`, `confirm` blocking overlay-click, narrower default `maxWidth: 420` vs shadcn `sm:max-w-lg` 512, no close X button).
- `PearlKit.internal.PearlAlertIcon` — Canvas-based tinted-badge atom with per-variant glyph (lowercase `i` for info, `!` for warning, `×` for error, `?` for confirm). 48×48 `Item` with a `Tokens.radius.md` `Rectangle` background at variant-tinted color (primary / warning / destructive @ 15 % α, or neutral `Tokens.muted` for confirm) and a centered 24×24 Canvas glyph stroked in the full-alpha variant color. Mirrors the `PearlXIcon` / `PearlCheckIcon` / `PearlChevron` pattern — no SVG assets, no Qt5Compat dependency. Repaint triggered via `Connections { target: root; function onVariantChanged() }` so glyph re-renders when the parent switches variants at runtime.
- `PearlKit.StatusBar` — fifteenth public component. Desktop main-window footer primitive with three regions: left-aligned hint, absolutely centered measurement, right-aligned status. Four `statusKind` values (`default` / `success` / `warning` / `error`) drive a derived `readonly property color statusColor` that consumers bind from the right slot for semantic coloring (mutedForeground / success / warning / destructive). Root is a plain `Rectangle` (no `QtQuick.Templates` base — a status bar has no interactive contract) filled with `Tokens.muted` and capped by a 1 px top hairline in `Tokens.border`. Three `property Component` slots (`leftContent`, `centerContent`, `rightContent`) instantiated through sibling `Loader`s anchored to the left edge, absolute horizontal center, and right edge respectively — absolute centering (not flexbox spacing) keeps the measurement fixed in the middle of the window even when left/right widths diverge (macOS Finder / Xcode convention). `hpad: int` (default 12, `Tokens.space.x3`) controls the outer slots' side inset. Implicit size 28 × 480 px — height matches the macOS footer idiom (between Xcode 23 px and Finder 24 px with slightly more breathing room for 14 px body text callers), width is a placeholder intended to be overridden via `Layout.fillWidth: true` or `anchors.left`/`anchors.right`. Disabled state fades each Loader independently to 50 % alpha without touching the root control — preserves child atoms (e.g. a focus ring around a clickable hint) from parent-opacity inheritance. Background color transition animated at `Tokens.motion.fast` (150 ms) for theme-switch smoothness. No shadcn/ui equivalent — shadcn targets web-first layouts where persistent footers are rare; design language is taken from the macOS footer idiom and documented as a deliberate divergence.
- `PearlKit.Spinner` — fourteenth public component. Small indeterminate activity indicator for inline loading states — a 3/4 circle arc that rotates continuously at 1 revolution per second (shadcn `animate-spin` parity). Root is a plain `Item` (no `QtQuick.Templates` base — spinners have no interactive contract). Arc rendered through a child `Canvas` using `ctx.arc` with `lineCap: "round"` (mirrors lucide `Loader2Icon`'s round stroke caps), sweeping 270° from top (−π/2) for a clean symmetric gap; `strokeWidth: 2` default matches lucide's stroke-2 at viewBox-24. Rotation driven by `RotationAnimator on rotation` attached to the Canvas — scenegraph-thread animation, so the Canvas paints once and only the transform ticks per frame. Properties: `color: Tokens.mutedForeground` (theme-reactive via Canvas `Connections` → `requestPaint()`), `strokeWidth: 2`, `running: true`, `duration: 1000` ms. Implicit size 16 × 16 (shadcn `size-4`); consumers resize via `width` / `height` directly — no string size scale. `running` gated by `control.visible` so hidden spinners don't burn scenegraph time. Accessibility via `Accessible.role: Accessible.Indicator` with `Accessible.name: "Loading"` (closest Qt equivalent to shadcn's `role="status"` + `aria-label="Loading"`). Deliberate divergences from shadcn: 270° arc instead of lucide's ~288° (visually imperceptible at 16–24 px, cleaner geometry); `running` and `duration` exposed (shadcn spins unconditionally at a fixed 1 s); explicit `color` property (QML has no CSS `currentColor` equivalent); no string size scale — `width` / `height` overrides cover every observed use case.
- `PearlKit.ProgressBar` — thirteenth public component. Thin pill-shaped horizontal progress indicator with both determinate and indeterminate modes. Defaults: `from: 0`, `to: 100`, `value: 0`, `implicitWidth: 200`, `implicitHeight: 8` (shadcn `h-2` parity); `to: 100` overrides `T.ProgressBar`'s native `to: 1` for a user-friendlier "45 means 45%" convention. Built on `QtQuick.Templates.ProgressBar` with `background` and `contentItem` overridden: background is a pill `Rectangle` (`radius: height / 2`) painted at `Tokens.primary @ 20 % alpha` (shadcn `bg-primary/20`), `clip: true` mirrors shadcn's `overflow-hidden`; contentItem is zeroed (`Item { }`) so the fill lives entirely inside the clipped background region. Two visibility-gated indicator rectangles inside the track — determinate indicator is full track width translated left by `-(1 - visualPosition) * width` (mirrors shadcn's `translateX(-${100 - value}%)` technique; `Behavior on x` at `Tokens.motion.fast` / 150 ms `Easing.OutCubic`, matches shadcn `transition-all`), indeterminate indicator is 40 % track width animated from `-indicator.width` to `track.width` on a 1800 ms `Easing.InOutCubic` infinite-loop `NumberAnimation` (`running:` gate turns animation off when `!indeterminate || !visible`, keeps idle CPU quiet). `focusPolicy: Qt.NoFocus` (non-interactive — no hover / pressed / focus state, no focus ring). Disabled state fades the track `background` to 50 % opacity; root control opacity untouched per pearl-kit rule. Deliberate divergences from shadcn: `indeterminate: bool` mode is pearl-kit-original (shadcn's Progress only renders `value || 0`, so undefined value becomes empty), added for DALI worker-status call sites where completion target is unknown; `to: 100` default instead of `T.ProgressBar`'s `to: 1`; `implicitWidth: 200` instead of shadcn's `w-full` (QML lacks a direct fill-parent equivalent — consumers set `Layout.fillWidth: true` or explicit width).

## [0.2.0] — 2026-04-18

### Changed

- **Breaking**: `PearlKit.Text` renamed to `PearlKit.PearlText`. The previous `Text` identifier shadowed `QtQuick.Text` under an unqualified `import PearlKit 1.0`, forcing consumers into `import PearlKit 1.0 as P` even for typography-only use. `PearlText` avoids the shadow so pearl-kit typography can be mixed with raw `Text` elements without namespace ceremony. Consumers upgrading from `0.1.0`: replace `P.Text { ... }` with `P.PearlText { ... }` (or use the unqualified `PearlText { ... }` if you only import pearl-kit once). The internal atom `PearlKit.internal.PearlText` was renamed to `PearlKit.internal.PearlBaseText` to free the name; this atom is not part of the public API.

### Added

- `PearlKit.ScrollArea` — twelfth public component. Scrollable viewport with thin, rounded auto-hiding scrollbars matching shadcn/ui v4 (`w-2.5` track, `p-px` padding, `rounded-full bg-border` thumb). Built on `QtQuick.Templates.ScrollView` with both `ScrollBar.vertical` and `ScrollBar.horizontal` attached properties replaced by custom `T.ScrollBar` instances (inline `contentItem` Rectangle as the thumb, transparent `Item` background, `minimumSize: 0.1` so the thumb stays grabbable on very long content). Single `autoHide: bool` (default `true`) drives Radix-style fade: scrollbar opacity = 0 when `!control.hovered && !bar.active && !bar.hovered`, else 1, transitioned at `Tokens.motion.base` (180 ms) with `Easing.OutCubic`. Thumb color states — idle `Tokens.border`, hover `Tokens.mutedForeground @ 80 %`, pressed `Tokens.mutedForeground` — transition at `Tokens.motion.fast` (150 ms). Requires `QtQuick.Controls` import alongside `QtQuick.Templates` because the `ScrollBar.*` attached property comes from Controls, not Templates. Deliberate divergences from shadcn: no focus-visible ring on the viewport — `T.ScrollView` silently reparents direct children into its internal `Flickable.contentItem`, which would cause `PearlFocusRing` to scroll with content instead of wrapping the viewport; introducing an Item wrapper to hoist the ring would break `Layout.fillWidth` / attached-property ergonomics, so the ring is dropped (keyboard scrolling feedback is carried by the thumbs themselves); hover / pressed thumb tint is pearl-kit-specific (shadcn ships only `bg-border`) because macOS / Windows desktop cursors expect thumb feedback; `minimumSize: 0.1` is a usability addition not present in shadcn / Radix.
- `PearlKit.Splitter` — resizable panel container for horizontal or vertical split layouts with persistable sizes. Single `withHandle: bool` property toggles a centered 16 × 12 grip-dot rectangle on each handle; default is a bare 1 px hairline in `Tokens.border` with a 6 px drag-hit area. `orientation: Qt.Horizontal | Qt.Vertical` (default `Qt.Horizontal`). Size persistence via convenience `saveLayout()` / `restoreLayout(state)` methods that pass through to Qt's native `T.SplitView.saveState()` / `restoreState()`. Built on `QtQuick.Templates.SplitView` with a custom `handle: Item` delegate — the 1 px-visible + 6 px-hit pattern is realized as an outer Item (hit area) with a centered 1 px Rectangle child (visual). `T.SplitHandle.pressed` / `T.SplitHandle.hovered` attached properties drive the hover-to-`Tokens.ring @ 60 %` transition (`Tokens.motion.fast`, 150 ms, `Easing.OutCubic`). Cursor shape swapped via an interior `MouseArea` with `acceptedButtons: Qt.NoButton` so native drag handling continues to work (pressing the MouseArea is a no-op, events propagate to `T.SplitView`). Focus ring is pearl-kit standard 3 px @ `ring / 50 %` with `offset: 0`, targeting the handle delegate root so Tab-navigation gives per-handle feedback. Orientation inside the handle is read as `control.orientation === Qt.Horizontal` via id reference — `T.SplitView.view` attached property does not resolve on handle delegates (canonical Qt idiom). Supports nesting for VS Code-style group layouts; each nested `Splitter` saves and restores independently. Disabled state fades the hairline and grip to 50 % alpha without touching the root control (standard pearl-kit rule — keeps the focus ring crisp). Deliberate divergences from shadcn's `resizable`: hover tint (`Tokens.ring @ 60 %`, VS Code-pattern; shadcn's default class has no explicit hover) driven by downstream airontgen editor-group use-cases; focus ring geometry (3 px / offset 0) for intra-kit consistency with Button / Input / Toggle / CheckBox instead of shadcn's literal `ring-1 ring-offset-1`; 6 px hit area instead of shadcn's 4 px for more forgiving pointer targeting on touch-screen Macs.
- `PearlKit.FormRow` — eleventh public component. Horizontal label-and-input layout primitive with optional `hint` and `error` text per row. Properties: `label: string`, `hint: string`, `error: string`, `labelWidth: int` (default `120`). Default content slot accepts one child Item (the input); the slot auto-binds `children[0].width = Qt.binding(() => _slot.width)` so the input fills the available column automatically. Root is a plain `Item` wrapping a 2-column `GridLayout` (row 0: label + input, row 1: empty + hint/error at `Layout.column: 1`) because the label must vertically center against row 0 only — a nested `RowLayout` + `ColumnLayout` would drift the label downward when hint/error grows. Reuses `PearlText` with `variant: "label"` (14 px / medium / `leading-none`, shadcn Label parity) for the label and `variant: "muted"` (14 px / regular / `mutedForeground`, shadcn FormDescription parity) for the meta row. When `error.length > 0`, label and meta text recolor to `Tokens.destructive` (shadcn `FormMessage` + `FormLabel data-[error=true]:text-destructive` parity). Color transitions animate at `Tokens.motion.fast` (150 ms). Disabled state fades label + meta text to 50 % alpha and does not auto-propagate to the input child (consumer responsibility, matching shadcn). Deliberate divergences from shadcn: horizontal layout instead of shadcn's vertical stack — desktop form idiom; single primitive instead of shadcn's seven-component `Form` / `FormField` / `FormItem` / `FormLabel` / `FormControl` / `FormDescription` / `FormMessage` tree (pearl-kit has no `react-hook-form` equivalent, so the state-provider layers have no target to bind); no `aria-describedby` auto-wiring (QML uses `Accessible.description` on the input child instead of HTML ARIA); input child width is auto-bound rather than style-driven (`w-full` in shadcn).
- `PearlKit.Card` — tenth public component. Elevated surface primitive — a bordered, padded container with an optional left-border accent stripe for status signaling. Single `variant` property across `default` / `destructive` / `warning` / `success` / `info` drives the accent color; non-default variants auto-enable a 4 px stripe (overridable via `accentWidth`). Root is a plain `Item` (no `QtQuick.Templates` base — a card has no interactive contract). Surface is a child `Rectangle` with `radius: Tokens.radius.xl` (12 px, shadcn `rounded-xl` parity), `border.color: Tokens.border`, `border.width: 1`, and `clip: true` so the accent stripe's left corners round with the card's outer radius (cleanest QML idiom for single-side rounded corners). Content `Column` is a sibling of the surface (not a child, mirrors `GroupBox`) — offset inwards by `padding` on all four sides, children spaced at `spacing` default 24 px for shadcn `flex flex-col gap-6` parity. `padding` and `spacing` overridable per-instance for compact layouts. Disabled state fades background + content column to 50 % alpha; root opacity untouched (standard pearl-kit rule). Deliberate divergences from shadcn: single atomic component instead of shadcn's seven-primitive composition (`Card` + `CardHeader` + `CardTitle` + `CardDescription` + `CardAction` + `CardContent` + `CardFooter`) — `GroupBox` already covers titled/header semantics, and QML `Column` + `Separator` replace the flexbox divider affordance; pearl-kit-original `variant`-driven left-border accent stripe (no shadcn equivalent) motivated by DALI's `ImplantCard` invalid-collision state; no `shadow-sm` (consistency with `GroupBox` — border + background read as elevated on macOS); built-in `Column` layout so `Card { PearlText {} PearlText {} }` just works without a wrapper.
- `PearlKit.Separator` — ninth public component. Thin 1 px divider with `orientation: "horizontal" | "vertical"` (default `"horizontal"`) and `decorative: bool` (default `true`) properties. Root is a plain `Rectangle` with `color: Tokens.border` — no `QtQuick.Templates.*` base because separators have no interactive state. When decorative, `Accessible.role` is `NoRole` so assistive tech skips it; when `decorative: false`, role becomes `Accessible.Separator`. Implicit thickness axis is `1`, non-thickness axis is `0` — consumers set the long axis via `Layout.fillWidth` / `Layout.fillHeight`, anchors, or explicit `width` / `height`. Deliberate divergences from shadcn: pearl-kit does not auto-fill the long axis (shadcn uses Tailwind `w-full` / `h-full`); QML expects container-aware sizing via layouts or anchors, which is the standard Qt pattern.
- `PearlKit.GroupBox` — eighth public component. Titled section container for grouping form rows under a heading, with optional collapse and a built-in "advanced" visibility gate. Properties: `title: string`, `description: string`, `collapsible: bool` (default `false`), `expanded: bool` (default `true`, read-write), `advanced: bool` (default `false`), `advancedVisible: bool` (default `true`). Default content slot lands children in an inner `Column` at 12 px spacing. Collapse runs three concurrent animations (`height`, `opacity` on the inner wrapper, plus outer `Column.spacing`) at `Tokens.motion.base` (180 ms) with `Easing.OutCubic`; the wrapper is `clip: true` so children don't leak during the height transition. Visibility gate computes `visible: !advanced || advancedVisible` so `advanced && !advancedVisible` collapses the layout slot entirely (DALI `_mark_advanced` parity). Root is a plain `Item` (not `T.GroupBox`) for full control over the collapse animation. Reuses `PearlText` (title / description), `PearlChevron` (rotates 180° on expand via `Behavior on rotation`), and `PearlFocusRing` (3 px @ `ring/50`, `offset: 2`, visible only when `collapsible && activeFocus`). Header click region is an inline `MouseArea` with `PointingHandCursor`; keyboard activation via `Keys.onSpacePressed` / `onReturnPressed` on the root, gated by `collapsible`. Disabled state fades header + body separately, never the root, so the focus ring stays crisp. Deliberate divergences from shadcn: pearl-kit-original component (no shadcn `GroupBox` primitive — composed from shadcn `Card` visuals + `Accordion` collapse semantics + DALI advanced-settings gate); `Tokens.radius.lg` (8 px) instead of shadcn `Card`'s `rounded-xl` (12 px) for a tighter form-section feel; no `shadow-sm` — settings sections sit flat against the tab background; no header-action or footer slot (deferred).

## [0.1.0] — 2026-04-18

### Added

- `PearlKit.Button` — first public component. Six variants (`default`, `destructive`, `outline`, `secondary`, `ghost`, `link`) x five sizes (`default`, `xs`, `sm`, `lg`, `icon`) with `iconLeft` / `iconRight` URL slots. Built on `QtQuick.Templates.Button` with state-reactive background and foreground color transitions, keyboard-only focus ring (3px, 50% ring-color alpha, no offset), and shadcn/ui v4 visual parity.
- `PearlKit.Input` — second public component. Single 36 px size (shadcn parity — Input has no size variants), `property bool error` for invalid-state visuals (border + ring switch to destructive), `iconLeft` / `iconRight` URL slots (pearl-kit extension over shadcn for Button consistency), theme-aware background (transparent in light mode, `Tokens.input` at 30% alpha in dark modes). Built on `QtQuick.Templates.TextField` with the default `TextInput` contentItem preserved for native caret, IME, selection, and undo/redo support. Focus ring appears on any focus source (mouse or keyboard) rather than `visualFocus`-only as Button — text inputs need caret feedback on click too.
- `PearlKit.Toggle` — third public component. iOS-style switch with two sizes (`sm`, `default`) matching shadcn/ui v4 Switch geometry (24×14 and 32×18 px tracks, 12 and 16 px thumbs). Built on `QtQuick.Templates.Switch` with a custom `indicator` slot (rounded-full track + animated thumb). Keyboard-only focus ring (3 px @ `ring/50`, offset 0) targets `control.indicator` so the ring follows the rounded-full track radius instead of the default rectangular fallback. Thumb position animated via `Behavior on x` (NumberAnimation) and color via `Behavior on color` (ColorAnimation), both at `Tokens.motion.fast` with `Easing.OutCubic`. Named `Toggle` rather than shadcn's `Switch` for pearl-kit roadmap and DALI migration consistency. Thumb color uses a uniform `foreground` / `primaryForeground` formula across all themes instead of a `Tokens.isLight`-branched approach — deliberately higher-contrast in Light mode than shadcn. Track unchecked background uses full-alpha `Tokens.input` across all modes (shadcn uses `dark:/80`). No `shadow-xs` (skipped by design, negligible on a control this small).
- `PearlKit.Select` — fourth public component. Non-editable dropdown with two sizes (`sm`, `default`), `property bool error` for invalid-state visuals, model-driven API (supports string arrays, ListModel, and JS object models via `textRole` / `valueRole`), popper-positioned popup with fade + scale enter transition (`Tokens.motion.fast`), and `type`-role grouped models (`"item"` / `"header"` / `"separator"`) as a unified replacement for shadcn's `SelectLabel` + `SelectSeparator` + `SelectGroup` primitives. Built on `QtQuick.Templates.ComboBox` with every slot overridden: `background` (Input-style rectangle), `contentItem` (PearlText with placeholder handling), `indicator` (new `PearlChevron` internal atom), `popup` (inline `T.Popup` parented to `T.Overlay.overlay`), and `delegate` (`T.ItemDelegate` branching on the row type). Commits to header or separator rows are guarded by an `onActivated` interceptor that reverts to the last valid selection. Keyboard navigation (Up / Down / Home / End / Space / Return / Escape / type-ahead) is handled natively by `T.ComboBox`; note that Qt's built-in nav does not skip non-selectable rows, so the keyboard highlight passes through headers and separators — the commit guard handles this cleanly. Deliberate divergences from shadcn: popper-only positioning (no item-aligned), static 320 px popup max height, no ScrollUp / ScrollDown chevron buttons (native `ScrollIndicator` used instead), minimal filled-dot current-row indicator instead of a check glyph, no `shadow-md` on the popup.
- `PearlKit.internal.PearlChevron` — Canvas-based stroked chevron atom with `strokeColor`, `strokeWidth`, and `direction` (`"down"` / `"up"`) properties. Avoids adding a new Qt.labs dependency for Select's indicator. Re-usable for future components that need a chevron glyph.
- `PearlKit.Dialog` — fifth public component. Modal popover with `title`, `description`, default body slot, `footer: Component`, `showCloseButton`, `maxWidth`, and `initialFocusItem` properties. Built on `QtQuick.Templates.Popup` (not `T.Dialog` — `T.Dialog`'s `standardButtons` + `accepted()` / `rejected()` ecosystem is tied to a fixed Ok / Cancel taxonomy that shadcn does not use). Overlay is a 50 %-black `T.Overlay.modal` `Rectangle` with a 200 ms fade via `Behavior on opacity`. Enter / exit transitions run parallel `opacity` (0 → 1) and `scale` (0.95 → 1.0) `NumberAnimation`s over 200 ms with `Easing.OutCubic`, matching shadcn's `fade-in-0 zoom-in-95` / `fade-out-0 zoom-out-95`. Background shadow uses `QtQuick.Effects.MultiEffect` (Qt 6 official path; `Qt5Compat.GraphicalEffects.DropShadow` is deprecated). Close button is a 16×16 `Rectangle` with a new `PearlXIcon` Canvas atom centered inside and a 2 px focus ring at 2 px offset. Body slot uses `default property alias _bodyContent: bodyColumn.data` — consumer children land in a 16 px-spaced body Column. Footer is a `property Component` rendered through a `Loader` for maximum composition flexibility. Positioning is manual `Math.round((parent.dim - own.dim) / 2)` since `T.Popup` does not expose `anchors`. Deliberate divergences from shadcn: single component instead of five separate React primitives, `T.Popup` base instead of `T.Dialog`, single-layer shadow, `maxWidth` as an int property instead of a Tailwind responsive class.
- `PearlKit.internal.PearlXIcon` — Canvas-based stroked X atom with `strokeColor`, `strokeWidth` properties. Mirrors the `PearlChevron` pattern; avoids shipping an SVG asset or taking a dependency on Lucide / Qt5Compat. Reusable by any future component needing an X glyph (close buttons, dismiss badges, etc.).
- `PearlKit.CheckBox` — sixth public component. 16×16 rounded-square box with a lucide-style checkmark glyph on checked, horizontal dash on indeterminate. Single size (shadcn parity). Built on `QtQuick.Templates.CheckBox` with `property bool error` (mirrors `Input` / `Select` patterns) that switches border + focus ring to `destructive` color. `indicator` slot overridden as a `Rectangle` with the check glyph centered inside; `contentItem` zeroed out to force indicator-only rendering (label composition is the consumer's responsibility, matching shadcn). Focus ring 3 px @ `ring / 50 %`, keyboard-only via `visualFocus`, target `control.indicator` so the radius wraps the 4 px box correctly. No `Behavior on color` — check / uncheck snaps instantly per shadcn's `transition-none` directive. Deliberate divergences from shadcn: `tristate` / indeterminate support (Radix primitive exposes it but shadcn doesn't stylize), no built-in label slot (external compose like shadcn). Error ring alpha is theme-aware (0.2 light / 0.4 dark) matching shadcn's `dark:ring-destructive/40`.
- `PearlKit.internal.PearlCheckIcon` — Canvas-based stroked check / dash atom with `strokeColor`, `strokeWidth`, and `mode` (`"check"` / `"dash"`) properties. Normalized coordinates scale to any size; `onWidthChanged` / `onHeightChanged` repaint ensures sharp render at non-default dimensions. Mirrors `PearlChevron` / `PearlXIcon` pattern. Reusable by any future component needing a check or dash glyph.
- `PearlKit.PearlText` — typography primitive (Tier 0 P0, most-used widget). Single `variant` property — `title`, `heading`, `body` (default), `muted`, `label`, `code`, `mono` — drives font family, size, weight, color, and line height from `Tokens.font.*` and `Tokens.foreground` / `Tokens.mutedForeground`. Root is a pure `QtQuick.Text` Item (no `Templates` base, no `Rectangle` wrapper) so every native property passes through — `elide`, `wrapMode`, `horizontalAlignment`, `maximumLineCount`, `textFormat`. `renderType: Text.NativeRendering` + `antialiasing: true` explicit for crisp Retina rendering. Disabled state fades color to 50 % alpha; font properties unchanged. Named `PearlText` rather than `Text` so it does not shadow `QtQuick.Text` under an unqualified `import PearlKit 1.0` — consumers can mix pearl-kit typography with raw `Text` elements without namespace ceremony. Deliberate divergences from shadcn: single component instead of `<Label>` + Tailwind typography classes (`variant: "label"` covers shadcn Label parity, `text-sm leading-none font-medium`); `code` variant renders mono-muted text without shadcn's `bg-muted` chip (pure `Text` Item cannot render a background — consumers wrap in a `Rectangle` if needed); `mono` variant is pearl-kit-specific (mono + foreground for data display vs. `code`'s mono + muted for documentation flavor); shadcn typography `h1..h4` / `lead` / `large` / `small` variants not shipped — not requested by DALI call-sites, can be added on demand.
- `PearlKit.Stepper` — seventh public component. `[−] value [+]` layout with dedicated decrement / increment buttons at the track edges. Single `value: real` API across integer and floating-point modes via a hidden integer-scaling layer (`decimals: 0` behaves as int; `decimals: 1..3` preserves the float precision by storing `Math.round(value * 10^decimals)` in the internal `T.SpinBox.value` int). Root is an `Item` wrapper around `T.SpinBox` because `T.SpinBox.value`'s int typing cannot be overridden in-place. Visual composition mirrors `Input` (36 px track, `Tokens.radius.md`, 1 px `Tokens.border`, theme-aware background) with two 32 px ghost-Button-style edge indicators (`Tokens.accent` hover tint at 40% / 60%). Edge buttons auto-disable when `value` hits `from` or `to` (DALI parity). `suffix` (e.g. `"%"`, `"px"`) and `specialValueText` (shown when `value == from`, e.g. `"Auto"`) bake into `textFromValue` / `valueFromText`; on focus-in, the line edit shows the raw value (no suffix) and `selectAll()` runs, on focus-out the format is re-bound. `Keys.onEscapePressed` restores the bound display and blurs. Focus ring 3 px @ `ring / 50 %` on any focus source (`activeFocus`, Input pattern), destructive-tinted when `error: true`. Validator swaps between `IntValidator` and `DoubleValidator` based on `decimals`. Derivative from shadcn primitives — shadcn/ui does not ship a stepper; behavioral reference is DALI's `DaliStepper` / `DaliDoubleStepper` (~20 call sites for zoom, dimensions, thresholds). Deliberate divergences documented: unified single-API real-valued `value`, `decimals` capped at 3 to avoid 32-bit overflow in the integer-scaled SpinBox, no `wrap`, ghost-button hover tint on the edge buttons.
- `PearlFocusRing` parameterized: new `ringWidth` (default 2) and `ringColor` (default `Tokens.ring`) properties for per-component ring customization. Backward-compatible with existing defaults.

### Changed

- `Tokens.motion.fast` updated from 120ms to 150ms to match the shadcn / Tailwind transition duration default so future components stay consistent.
- Default theme flipped from `DarkBlue` to `Light` (`Tokens.mode` initial value). `DarkBlue` remains selectable at runtime via `P.Tokens.mode = P.Tokens.DarkBlue`; no palette or enum order changes.

### v0.0.0 — Initial infrastructure

- Initial project scaffolding (uv + hatchling + hatch-vcs).
- `Tokens` QML singleton with shadcn taxonomy and airontgen-derived palette (Dark / DarkBlue / Light).
- `PearlKit.internal` helper atoms: `PearlBackground`, `PearlBaseText`, `PearlFocusRing`, `PearlPopup`.
- `pearl_kit.register_qml(engine)` Python API for QML module discovery.
- CI, release, and docs GitHub Actions workflows.
- mkdocs-material documentation skeleton.
